<android.support.v4.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <TextView
            android:id="@+id/where_am_i"
            android:layout_width="match_parent"
            android:layout_height="40dp"
            android:textStyle="bold"
            android:textSize="25sp"
            android:textColor="@color/colorAccent"
            android:background="@color/white"
            android:fontFamily="casual"
            android:paddingLeft="10dp"
            android:text="Where am I" />

        <FrameLayout
            android:id="@+id/fragment_container"
            android:layout_below="@+id/where_am_i"
            android:layout_width="match_parent"
            android:layout_height="match_parent"/>

        <ImageButton
            android:id="@+id/bAddHashTag"
            android:layout_width="@dimen/add_hashtag_size"
            android:layout_height="@dimen/add_hashtag_size"
            android:layout_alignParentRight="true"
            android:layout_alignParentBottom="true"
            android:layout_margin="20dp"
            android:elevation="10dp"
            android:background="@drawable/round_button"
            android:src="@drawable/ic_add_pink_40dp_pad5dp"/>

    </RelativeLayout>


    <android.support.design.widget.NavigationView
        android:id="@+id/nav_view"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        app:menu="@menu/menu_app"
        android:fitsSystemWindows="true"/>

</android.support.v4.widget.DrawerLayout>
